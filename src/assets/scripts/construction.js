// Точка входу сторінки «Хід будівництва» (/constructions/).
//
// WP-тема підключає на кожній сторінці свій <slug>.bundle.js, і для
// /constructions/ це construction.bundle.js. У білді такого entry не було
// ніколи, тому сторінка віддавала 404 на скрипт (знайдено в SEO-аудиті).
// Своєї JS-логіки в сторінки немає: список звітів рендерить PHP, попапи й
// анімації йдуть з index.bundle.js. Тому бандл лишається порожнім — він
// потрібен тільки щоб запит віддавав 200.
//
// Якщо бекенд приберe wp_enqueue_script('construction') у темі — прибрати
// і цей файл, і entry 'construction' у webpack.config.js.
export {};
